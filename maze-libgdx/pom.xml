<!-- maze-libgdx/pom.xml -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>main.game.maze</groupId>
    <artifactId>MazeGame</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>maze-libgdx</artifactId>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <gdx.version>1.12.1</gdx.version>
  </properties>

  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo.maven.apache.org/maven2</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>main.game.maze</groupId>
      <artifactId>maze-common-frontend</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>main.game.maze</groupId>
      <artifactId>maze-common-backend</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- WallRegistry generated class -->
    <dependency>
      <groupId>main.game.maze</groupId>
      <artifactId>maze-module-generator</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Explicit declaration ensures WallMaterialBaseType is on the classpath.
         WallRegistry (generated) references this class at static-init time; without
         an explicit dep, eclipse-plugin packaging prevents transitive resolution in
         VS Code's Java extension and maven-dependency-plugin copy-dependencies. -->
    <dependency>
      <groupId>main.game.maze</groupId>
      <artifactId>main.game.maze.walls</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- mazeworld bundle for maze data model -->
    <dependency>
      <groupId>main.game.maze</groupId>
      <artifactId>main.game.maze.mazeworld</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- OCL delegate factories — system scope is not transitive, so these must be
         declared explicitly here even though maze-common-backend already declares them.
         Required for OclBootstrap.init() to succeed at test runtime
         (e.g. OclConstraintsParityTest). -->
    <dependency>
      <groupId>org.eclipse.ocl</groupId>
      <artifactId>org.eclipse.ocl</artifactId>
      <version>3.4.0-v20140524-1358</version>
      <scope>system</scope>
      <systemPath>${project.basedir}/../maze-common-backend/lib/org.eclipse.ocl-3.4.0-v20140524-1358.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>org.eclipse.ocl</groupId>
      <artifactId>org.eclipse.ocl.ecore</artifactId>
      <version>3.3.0-v20130520-1222</version>
      <scope>system</scope>
      <systemPath>${project.basedir}/../maze-common-backend/lib/org.eclipse.ocl.ecore-3.3.0-v20130520-1222.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>org.eclipse.ocl</groupId>
      <artifactId>org.eclipse.ocl.common</artifactId>
      <version>1.2.0-v20140528-1458</version>
      <scope>system</scope>
      <systemPath>${project.basedir}/../maze-common-backend/lib/org.eclipse.ocl.common-1.2.0-v20140528-1458.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>lpg.runtime</groupId>
      <artifactId>java</artifactId>
      <version>2.0.17-v201004271640</version>
      <scope>system</scope>
      <systemPath>${project.basedir}/../maze-common-backend/lib/java-2.0.17-v201004271640.jar</systemPath>
    </dependency>

    <dependency>
      <groupId>com.badlogicgames.gdx</groupId>
      <artifactId>gdx</artifactId>
      <version>${gdx.version}</version>
    </dependency>
    <dependency>
      <groupId>com.badlogicgames.gdx</groupId>
      <artifactId>gdx-backend-lwjgl3</artifactId>
      <version>${gdx.version}</version>
    </dependency>
    <dependency>
      <groupId>com.badlogicgames.gdx</groupId>
      <artifactId>gdx-platform</artifactId>
      <version>${gdx.version}</version>
      <classifier>natives-desktop</classifier>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>5.12.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>main.game.maze.libgdx.GdxAppLauncher</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.1</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/libs</outputDirectory>
              <includeScope>runtime</includeScope>
            </configuration>
          </execution>
          <execution>
            <id>copy-system-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/libs</outputDirectory>
              <includeScope>system</includeScope>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>

    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.eclipse.m2e</groupId>
          <artifactId>lifecycle-mapping</artifactId>
          <version>1.0.0</version>
          <configuration>
            <lifecycleMappingMetadata>
              <pluginExecutions>
                <pluginExecution>
                  <pluginExecutionFilter>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <versionRange>[3.6.1,)</versionRange>
                    <goals>
                      <goal>copy-dependencies</goal>
                    </goals>
                  </pluginExecutionFilter>
                  <action>
                    <ignore/>
                  </action>
                </pluginExecution>
              </pluginExecutions>
            </lifecycleMappingMetadata>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>coverage</id>
      <activation>
        <property><name>env.CI</name></property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <!-- Most libGDX rendering classes require a GL context and cannot be
                 tested headlessly. The 35 % actual coverage is near the CI limit;
                 use 0.30 to stay achievable while still guarding regressions. -->
            <executions>
              <execution>
                <id>jacoco-check</id>
                <phase>verify</phase>
                <goals><goal>check</goal></goals>
                <configuration>
                  <rules>
                    <rule>
                      <element>BUNDLE</element>
                      <limits>
                        <limit>
                          <counter>LINE</counter>
                          <value>COVEREDRATIO</value>
                          <minimum>0.30</minimum>
                        </limit>
                      </limits>
                    </rule>
                  </rules>
                  <excludes>
                    <exclude>main/game/maze/difficulties/impl/**</exclude>
                    <exclude>main/game/maze/opponents/impl/**</exclude>
                    <exclude>main/game/maze/difficulties/util/**</exclude>
                  </excludes>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
