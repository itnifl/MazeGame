# Requirements Traceability Matrix

## Scope

This matrix tracks movement and enemy-path overlay rules implemented in the
shared movement services and consumed by both JavaFX and libGDX frontends.

## Traceability

| Requirement | Design Pattern / Principle | Implementation | Verification Tests |
|---|---|---|---|
| GR-31, GR-32 | Shared service abstraction, single responsibility | `AdaptiveAggressiveMovementService.tick(...)` and `modeForEnemy(...)` | `AdaptiveAggressiveMovementServiceTest.afterFourSecondsStuckEnemyUsesShortestPathThenRecoversProgress`, `AdaptiveAggressiveMovementServiceTest.pathFollowModeRemainsActiveForTwentySecondsThenReturnsToDirectional` |
| GR-33 | Shared policy service, DRY parity across frontends | `AntiLoopWanderMovementService.tick(...)`, `PatrolMovementService.tick(...)` | `AntiLoopWanderMovementServiceTest.wanderBreaksOutOfTinyLoopArea`, `PatrolMovementServiceTest.noPathTriggersFiveSecondWanderThenRetriesWhenPathOpens` |
| GR-34 | Runtime state projection, backend-neutral DTO | `ActivePathPoint`, `AdaptiveAggressiveMovementService.currentPathForEnemy(...)`, `PatrolMovementService.currentPathForEnemy(...)`, JavaFX `GameController.drawEnemyNavigationPaths(...)`, libGDX `GdxGameScreenController.drawEnemyPathOverlay(...)` | `AdaptiveAggressiveMovementServiceTest.pathFollowModeRemainsActiveForTwentySecondsThenReturnsToDirectional` (live path non-empty assertion), `PatrolMovementServiceTest.shortestPathMovesAroundBarrierInsteadOfPushingIntoIt` (live path non-empty assertion) |
| GR-35 | Deterministic core flow with constrained random turn choice | `AntiLoopWanderMovementService.pickRandomCardinal(...)` | `AntiLoopWanderMovementServiceTest.continuesStraightUntilCollisionThenTurns`, `AntiLoopWanderMovementServiceTest.allowsReverseWhenThatIsTheOnlyOpenDirection` |
| GR-17, GR-18 | Shared domain service, DRY parity across frontends | `ScoringEngine.gameplayScore(...)`, `ScoringEngine.characterScreenScore(...)`, JavaFX `CharacterActionScreens.updateScore(...)`, libGDX `GdxGameScreenController.currentScore(...)` and `baseScoreForDifficulty(...)` delegating to `GameScoringConstants.baseScoreFor(...)` | `ScoringEngineTest.gameplayScoreUsesMoveAndHintPenalties`, `ScoringEngineTest.characterScreenScoreAppliesDeathAndWinRules`, `GameControllerScoringParityTest`, `GdxScoringAndSpawnParityTest.libgdxBaseScoreUsesSharedConstantsForAllDifficulties` |
| SR-9 | Session budget service, single responsibility | `PathHintBudget`, libGDX `GdxGameScreenController.applyPathPenalty(...)` integration | `PathHintBudgetTest.consumeStopsAtBudgetAndMarksExhausted`, `PathHintBudgetTest.exhaustedBudgetConsumesNothing`, `GameControllerPathHintBudgetTest` |
| GR-24 | Director orchestration, frontend-agnostic audio transitions | `GameAudioDirector` used by libGDX `GdxGameScreenController.switchToInGameMusic(...)`, `switchToMenuMusic(...)`, win/game-over transitions | `GameAudioDirectorTest.switchToInGameStopsOtherChannelsThenLoopsInGameMusic`, `GameAudioDirectorTest.switchToWinMusicPlaysWinComment` |
| GR-15, GR-16 | MVC view decomposition, separation of concerns | libGDX `GdxStartMenuView`, `GdxHudView`, `GdxGameWorldView`, `GdxOverlayView` orchestrated by `GdxGameScreenController.draw()` | `GdxGameScreenLayoutTest`, `GdxGameScreenParityTest` |
| Runtime movement orchestration | Shared interface + director for enemy tick lifecycle | `EnemyRuntime`, `EnemyDirectorService`, libGDX `GdxEnemyRuntime`, `GdxGameScreenController` enemy advance delegation | `EnemyDirectorServiceTest.advanceAllCallsEachEnemyOnce`, existing libGDX parity suites (`GdxMovementParityTest`, `DifficultyXmiParameterizedParityTest`) |
| High score persistence parity | Repository pattern, shared file IO helper | `HighScoreRepository`, `FileHighScoreRepository`, JavaFX `HighScoreController.loadScoresFromFile(...)`, JavaFX `ActionScreenController.upsertScore(...)`, libGDX `GdxGameScreenController.loadHighScores(...)` / `writeHighScore(...)` | `FileHighScoreRepositoryTest.upsertAndLoadScoresAreSortedDescending`, `HighScoreFileTest`, `HighScoreControllerTest` |
| SR-22 (partial) | Shared session aggregate, single source of runtime mode state | `GameSession`, `GameMode`, libGDX `GdxGameScreenController` mode and session flag migration | `GdxGameScreenParityTest`, `GdxGameScreenLayoutTest`, `GdxGameScreenTerminalCommandTest` |
| HUD status message lifecycle | Shared message bus abstraction | `StatusMessageBus`, libGDX `GdxGameScreenController.flashStatus(...)` / `update(...)` | `StatusMessageBusTest.publishedMessageExpiresAfterTick` |
| Terminal overlay discoverability | Command contract parity across frontends | libGDX `GdxGameScreenController.parseTerminalCommand(...)` and `terminalHelpText()`, JavaFX terminal command parser/help text | `GdxGameScreenTerminalCommandTest.parsesShowEnemyPathCommand`, `GdxGameScreenTerminalCommandTest.helpTextMentionsEnemyPathDuration` |
| SR-24 (proposed) | Adapter pattern, incremental migration | libGDX `GdxGame` + `MenuScreenController` + `PlayScreenController` + `ApplicationAdapterScreen` + `LegacyPlayScreenController` route existing `GdxGameScreenController` through `Game.setScreen(...)` | `GdxGameRoutingTest.createRoutesToInitialScreenFromFactory`, `GdxGameRoutingTest.createProvidesAssetServiceToInitialScreenFactory`, `GdxGameRoutingTest.defaultCreateRoutesToLegacyPlayScreenAndBuildsContext`, `GdxGameRoutingTest.routeToLegacyPlayUsesLegacyPlayScreen`, `GdxGameRoutingTest.routeToPlayScreenUsesPlayScreen`, `ApplicationAdapterScreenTest.showInitializesDelegateOnlyOnce`, `ApplicationAdapterScreenTest.renderAndResizeDelegateToApplicationAdapter`, `ApplicationAdapterScreenTest.disposeDelegatesToApplicationAdapter` |
| SR-25 (proposed) | Repository boundary around assets, SRP | libGDX `GdxAssetService` centralizes texture path normalization and AssetManager ownership | `GdxAssetServiceTest.normalizeInternalPathRemovesLeadingSlash`, `GdxAssetServiceTest.normalizeInternalPathKeepsRelativePath`, `GdxAssetServiceTest.normalizeInternalPathReturnsNullForBlankInput`, `GdxAssetServiceTest.getTextureReturnsNullWhenGdxFilesNotAvailable`, `GdxAssetServiceTest.queueTextureAndDisposeAreSafeWhenGdxFilesNotAvailable`, `GdxGameRoutingTest.disposeDisposesAssetService` |
| SR-26 (proposed) | Context object, dependency inversion for screens | `GdxGameContext`, `AbstractGdxScreenController` as shared screen boundary | `GdxGameContextTest.recordExposesRuntimeConfigAndAssets`, `AbstractGdxScreenTest.contextIsAvailableToSubclasses`, `AbstractGdxScreenTest.defaultLifecycleMethodsAreNoOp` |
| SR-29 (proposed) | DRY collaborator extraction, single responsibility | libGDX `DifficultyBoardConfig` centralizes board dimensions and board-size labels; shared backend `DifficultyPresentationSupport` centralizes difficulty display-name and board dimension policy for libGDX and JavaFX | `DifficultyBoardConfigTest.boardSizeUsesEasyDefaultsWhenDifficultyIsNull`, `DifficultyBoardConfigTest.boardSizeMatchesNormalDifficultyConstants`, `DifficultyBoardConfigTest.boardSizeMatchesHardDifficultyConstants`, `DifficultyPresentationSupportTest.boardSizeUsesEasyDefaultsWhenDifficultyIsNull`, `DifficultyPresentationSupportTest.displayNameUsesDifficultyModelNameAndFallback` |
| SR-30 (proposed) | Shared fallback policy, DRY config loading | libGDX `GdxVisualStyleSupport` centralizes visual-style loading policy (XMI then properties then default) and is consumed by `GdxGameScreenController`, `MenuScreenController`, and `GdxAppLauncher` | `GdxVisualStyleSupportTest.returnsPrimaryLoaderValueWhenAvailable`, `GdxVisualStyleSupportTest.fallsBackToPropertiesLoaderWhenPrimaryFails`, `GdxVisualStyleSupportTest.returnsDefaultAndRunsHookWhenAllLoadersFail` |
| SR-31 (proposed) | Command interpreter boundary, SRP | libGDX `GdxTerminalCommandSupport` centralizes terminal command parsing and command-to-outcome mapping used by `GdxGameScreenController` | `GdxTerminalCommandSupportTest.parseDelegatesToTerminalParser`, `GdxTerminalCommandSupportTest.evaluateReturnsExpectedActionsAndMessages`, `GdxTerminalCommandSupportTest.helpOutcomeUsesExtendedStatusDurationAndHelpText`, existing `GdxGameScreenTerminalCommandTest` delegation coverage |
| SR-32 (proposed) | Runtime state boundary, single responsibility | libGDX `GdxDebugOverlayState` centralizes temporary debug overlay timer state (behaviour label, movement label, enemy path overlay) and is consumed by `GdxGameScreenController` | `GdxDebugOverlayStateTest.terminalActionsActivateExpectedOverlays`, `GdxDebugOverlayStateTest.tickExpiresOverlayFlagsOverTime`, `GdxDebugOverlayStateTest.resetClearsAllOverlayState` |
| SR-33 (proposed) | Score flow boundary, repository abstraction | libGDX `GdxScoreSupport` centralizes current-score calculation and high-score persistence helpers used by `GdxGameScreenController` | `GdxScoreSupportTest.currentScoreDelegatesToSharedScoringEngine`, `GdxScoreSupportTest.loadHighScoresDelegatesToRepository`, `GdxScoreSupportTest.saveHighScoreDelegatesToRepository` |
| SR-34 (proposed) | Win score flow boundary, single responsibility | libGDX `GdxWinScoreSupport` centralizes win-name input sanitization, trimming, backspace handling, and score save preparation used by `GdxGameScreenController` | `GdxWinScoreSupportTest.appendCharacterAcceptsOnlyAllowedCharactersAndHonorsMaxLength`, `GdxWinScoreSupportTest.backspaceAndPlayerNameTrimInput`, `GdxWinScoreSupportTest.saveScoreDelegatesThroughScoreSupport` |
| SR-35 (proposed) | Command and registry input boundary, open closed design | Shared generic input core promoted to `maze-common-frontend` under `main.game.maze.common.input` (`GameAction`, `KeyBindingRegistry<K>`, `InputFrame<K>`, `InputRouter<K>`, `EdgeKeyTracker<K>`) and `...input.command` (`GameCommand<K>`, `GameCommandContext`); libGDX consumes `K=Integer` via per-frontend `InputSnapshotReader`, command implementations in `input.command`, and routing integration in `GdxGameScreenController.update(...)` | `EdgeKeyTrackerTest`, `KeyBindingRegistryTest`, `InputRouterTest` (in `maze-common-frontend`), plus libGDX routing guards `GdxGameInputBindingsSupportTest`, `GdxGameScreenParityTest` |
| SR-36 (proposed) | Gameplay state boundary, single responsibility | libGDX `GameWorldModel` owns gameplay runtime state (maze, player, runtime model, goal, hp/tint/infection, death timers, path-hint budget, score penalty, enemy and path lists) separate from lifecycle, rendering, and input | `GameWorldModelTest`, plus parity and layout guards `GdxGameScreenParityTest`, `GdxGameScreenLayoutTest` |
| SR-37 (proposed) | Render pipeline boundary, MVC decomposition | libGDX `GdxGameRenderPipeline` orchestrates world, HUD, and overlays by consuming an immutable `RenderState`; `GdxGameRenderCoordinator` owns render-state snapshot assembly wiring; `GdxGameMouseInteractionCoordinator` owns gameplay mouse interaction wiring | `GdxGameRenderPipelineTest` (pure world-availability and high-scores routing decisions), plus render behavior guards `GdxGameScreenLayoutTest`, `GdxGameScreenTerminalCommandTest` |
| SR-38 (proposed) | Mode routing boundary, deterministic update dispatch | libGDX `GameModeRouter` and `PlayingModeController` centralize per-mode update dispatch and are consumed by `GdxGameScreenController` | `GameModeRouterTest`, `PlayingModeControllerTest`, existing mode behavior tests in `GdxGameScreenParityTest` |
| SR-39 (proposed) | Bootstrap boundary for session startup | libGDX `GameSessionBootstrapper` owns session start/reset state computation (arena reuse/build, runtime model load, speed/goal sizing, player spawn, session and collaborator resets); `GdxGameStartFlowRequestFactory` owns start-flow request assembly and `GdxGameStartFlowApplySupport.apply(...)` owns post-coordinator apply orchestration | `GameSessionBootstrapperTest`, `GameSessionStartCoordinatorTest`, plus gameplay parity guard `GdxGameScreenParityTest` |
| SR-40 (proposed) | Audio transition coordination boundary | libGDX `GdxGameAudioCoordinator` encapsulates in-game, menu, win, and game-over music transitions over `GameAudioDirector` and `MazeVisualStyleConfig` | `GdxGameAudioCoordinatorTest`, plus `GameAudioDirectorTest` and gameplay parity guard `GdxGameScreenParityTest` |
| SR-41 (proposed) | Constructor complexity reduction, parameter object pattern, code smell removal (CRR-15) | libGDX `GdxGameScreenOptions` (immutable options + builder) replaces the `GdxGameScreenController` telescoping-constructor chain; the controller now exposes a single canonical `GdxGameScreenController(GdxGameScreenOptions)` constructor, and call sites (`PlayScreenController`, `LegacyPlayScreenController`, `forHighScores`) read as self-documenting builder calls | `GdxGameScreenOptionsTest`, plus construction and parity guards `GdxDifficultyPropagationTest`, `GdxScoringAndSpawnParityTest`, `GdxGameScreenParityTest` |
| SR-43 (proposed, partial) | JavaFX gameplay input moves toward command-based dispatch replacing inline key action handling | JavaFX `GameControllerInputSupport` now dispatches non-movement actions through `JavaFxGameCommand` implementations and `JavaFxInputCommandContext` (`ShowHighScoreCommand`, `OpenDifficultyPickerCommand`, `ShowNavigationPathCommand`, `ShowSpanningTreeCommand`) while preserving existing sink-driven side effects | `GameControllerInputSupportTest`, `JavaFxInputCommandContextTest`, `JavaFxGameCommandsTest` |
| SR-42 (proposed, partial) | Shared input core consumed by both frontends, DRY, backend-neutral abstraction | The shared generic input core in `maze-common-frontend` is consumed by both frontends: libGDX with `K=Integer` and JavaFX with `K=javafx.scene.input.KeyCode`. JavaFX `JavaFxInputSnapshotReader` now produces the shared `InputFrame<KeyCode>` consumed by `GameController` movement polling; the JavaFX-local `JavaFxInputFrame` record was removed. Full JavaFX `KeyBindingRegistry`/`InputRouter` wiring remains plan Phase 3 | `JavaFxInputSnapshotReaderTest`, `GameControllerInputSupportTest`, plus shared-core tests `EdgeKeyTrackerTest`, `KeyBindingRegistryTest`, `InputRouterTest` |
| SR-44 (proposed, partial) | JavaFX gameplay state lives in a dedicated model separate from the controller's lifecycle/rendering/input concerns (SRP, MVC model extraction, code smell removal CRR-11) | JavaFX `FxGameWorldModel` owns the scoring/path-hint gameplay state (`playerMoveCount`, path-hint budget triple, route-hint penalty cluster, enemy-path overlay flags) and `resetScoringState()`; `GameController` delegates all reads/writes through it. Core entities (`playerCharacter`/`maze`/`winarea`/enemies) and `setupGame()` bootstrapping remain in the controller pending later Phase 1 increments | `FxGameWorldModelTest`, plus regression guards `GameControllerPathHintBudgetTest`, `GameControllerRoutePenaltyTest` |
| SR-42 | The Command and key binding registry input core is shared in `maze-common-frontend` and consumed by both frontends. | Promoted `common.frontend.input` core; `KeyBindingRegistry<K>`, `InputRouter<K>`, etc. | `KeyBindingRegistryTest`, `InputRouterTest`, `EdgeKeyTrackerTest` (in common-frontend); `JavaFxInputBindingsSupportTest` (JavaFX consumer); `GdxGameInputBindingsSupportTest` (libGDX consumer). |
| SR-43 | JavaFX gameplay input is handled via the shared key-binding registry and command objects, replacing the inline `handleKeyPressed` switch. | `JavaFxInputBindingsSupport`, `JavaFxInputCommandContext`, `GameCommand<KeyCode>` implementations, and `InputRouter<KeyCode>` integration in `GameController`. | `JavaFxInputBindingsSupportTest`, `JavaFxGameCommandsTest`, `JavaFxRouterIntegrationTest`. |
| SR-45 | JavaFX gameplay session start and reset shall be encapsulated in a dedicated bootstrapper boundary. | `FxGameSessionBootstrapper` initialises board sizing, background, world, player config, player character, canvases (z-ordered), and enemies. Spawning is injectable via `BiConsumer<EnemyRegistrar,Difficulty>` to isolate tests from EMF/XMI. `EnemyRegistrar` (ISP interface) decouples `OpponentRuntimeFactory` from `GameController`. Transient HUD messages are owned by `FxHudCoordinator`; path-hint input flow is owned by `FxPathHintCoordinator`; difficulty-picker dialog flow is owned by `FxDifficultyPickerSupport`. | `FxGameSessionBootstrapperTest` (5 tests: result population, canvas order, overlay transparency, repeatability, registrar wiring). |
| SR-46 | The JavaFX movement thread, animation timer, and watchdog are owned by a dedicated concurrency coordinator with unchanged lifecycle semantics. | `FxMovementLoopCoordinator` encapsulates all threading and timer logic; `GameController` delegates lifecycle to it. | `FxMovementLoopCoordinatorTest` (callback invocation, idempotency, stop and dispose semantics); `GameControllerDisposeTest` and `MazeCoreBehaviorTest` (integration regression). |
| SR-47 | JavaFX mode-specific update logic is dispatched by a deterministic mode state-machine router. | Shared `GameModeRouter` drives `FxPlayingModeController`, which owns input routing, route-hint penalty accrual, and movement throttling for the PLAYING mode. | `FxPlayingModeControllerTest` (null-player guard, non-null player post-guard path covering penalty/router/throttle/key-checks, update throttle, contract stub); `GameControllerRoutePenaltyTest` (penalty accrual via reflection into `FxPlayingModeController`). |
| SR-48 | JavaFX frame rendering is orchestrated by a dedicated render coordinator consuming viewport and world metrics. | `FxGameRenderCoordinator.computeCameraTranslation(...)` handles windowed vs fullscreen translation, player centre-follow, and edge clamping. | `FxCameraTranslationTest` (13 tests: null-guard, windowed, fullscreen-clamp, single-axis overflow, fits-viewport, world-smaller-than-viewport); `WindowedCameraAndAudioTest` (9 tests covering no-scroll windowed, fullscreen clamp, single-axis overflow, fits-viewport, world-smaller-than-viewport, and game-over audio). |
| CI coverage gate — maze-javafx-backend ≥ 60 % line coverage | TDD regression-safety net for core coordinators and lifecycle boundaries | New test classes: `FxEnemyCoordinatorTest` (lifecycle, overlay, infection warning, unregisterCharacter, stepAll with wander zombie via reflection); `GameControllerLifecycleTest` (EnemyRegistrar delegation — resolveSpawnPosition, registerComputerCharacter, showInfectionWarning, runComputerCharacters, unregisterComputerCharacter); `AppLogicTest` (displayName, getBoardMaxX/Y null-difficulty fallbacks); `OpponentRuntimeFactoryNullGuardTest` (null-registrar early return before OCL/XMI); `FxPositionBoundsTest` (11 position-bounds edge cases); `CharacterActionScreensTest` (scoring delegation); `ProgressBarStatePresenterTest` (null-guard paths). | All named test classes (> 300 total passing tests across the module). |
| SR-49 | JavaFX audio transitions are encapsulated behind a dedicated coordinator. | `FxGameAudioCoordinator` adapts the shared `GameAudioDirector` for JavaFX, managing music transitions for different game states. | `FxGameAudioCoordinatorTest`. |

| F25, GR-26, GR-27 | Shared service overload (DRY, OCP), SRP factory init | `GhostNonTangibilityService.calculateOpacity(double, int)` two-arg overload caps phasing opacity at `visibilityLevel/100`; `OpponentRuntimeFactory.registerGhostCharacter` sets initial node opacity from `Ghost.getVisibilityLevel()`; `FxEnemyCoordinator.drainNonTangientEnergy` uses two-arg overload for phasing→solid transitions; `GdxEnemyRuntime.renderOpacity()` uses two-arg overload; `EnemySpawn.visibilityLevel` carries the model value through the libGDX pipeline via `RuntimeVisualModelLoader.visibilityLevelFor(CharacterType)` | `GhostNonTangibilityServiceTest` (10 new two-arg cases), `GhostTangibilityTest` (4 new visibility tests), `GhostTangibilityParityTest` (7 new visibility/parity tests) |
| F8 (EnemyMaxCount caps enforced at runtime) | Shared helper (SRP, DRY), Strategy pattern | `EnemySpawnPlanner.clampToCapLimit(EnemyTypes, int, Map)` in `main.game.maze.opponents.util`; wired into `OpponentRuntimeFactory.spawnByTarget(...)` (JavaFX) and verified-correct in `RuntimeVisualModelLoader.loadEnemySpawns(...)` (libGDX) | `EnemySpawnPlannerTest.clampToCapLimit_*` (7 cases); `GdxScoringAndSpawnParityTest.clampToCapLimitEnforcesPerTypeCeilingIdenticallyAcrossFrontends` (parity contract) |
| GR-26, GR-27 | Shared service (SRP, DRY), Strategy pattern | `GhostNonTangibilityService.isPhasing(...)`, `drainEnergy(...)`, `calculateOpacity(...)` in `maze-common-frontend` | `GhostNonTangibilityServiceTest` (11 original tests), `GhostTangibilityParityTest.renderOpacityFormulaMatchesJavaFx` |
| GR-26, GR-28, GR-29 | Shared service, Strategy, Reset on session start | `GhostPhasingMovementService.tick(...)`, `reset()` in `maze-common-frontend`; integrated in `GameController` and `GdxGameScreenController` | `GhostPhasingMovementServiceTest` (8 tests), `GhostTangibilityParityTest.phasingGhostDealsDamage_libgdxMatchesJavaFx`, `GhostTangibilityTest.phasingGhostDealsNoDamageThroughWall` |
| GR-26 (damage semantics) | OCP: wall-bypass ≠ damage-bypass | `GhostCharacter.doPositionEvaluation(...)` (JavaFX) skips wall check when phasing; `PlayerCombatStateService` (libGDX) skips wall check when phasing; both still apply contact damage | `GhostTangibilityTest.phasingGhostDealsNoDamageThroughWall`, `PlayerCombatStateServiceTest.phasingGhostDoesDamage_evenWithoutWall`, `PlayerCombatStateServiceTest.phasingGhostBypassesWallCheck_dealsDamage` |

| SR-51 (new) | Chase movement correctness across both frontends (DRY parity, shared service) | `ChasePlayerMovementService.tick(...)` in `maze-common-frontend` | `ChasePlayerMovementServiceTest` (6 tests: horizontal/vertical chase, at-player position, alt-axis when blocked, all-blocked stays still, symmetric-board stable direction) |
| SR-52 (new) | Terminal command parsing contracts shared between frontends | `TerminalCommandParser.parse(...)`, `HELP_TEXT` constant in `maze-common-frontend` | `TerminalCommandParserTest` (14 tests: all named commands, aliases, case-insensitivity, whitespace trimming, null/empty guards, non-empty HELP_TEXT) |
| SR-53 (new) | Enemy composition resolver handles caps correctly during redistribution | `CompositionResolverImpl.resolve(...)` — fixed bug where `redistributeToTotal` violated caps | `CompositionResolverImplTest` (8 tests: single ratio, override precedence, total=enemyCount invariant, idempotent, unknown profile IAE, null/empty profiles NPE/ISE, caps respected) |
| SR-54 (new) | Difficulty service lifecycle and EMF model correctness | `DifficultyService.list()`, `getCurrent()`, `setCurrent(...)` in `maze-common-backend` | `DifficultyServiceTest` (5 tests: returns 3 difficulties, each non-null, getCurrent before set no-throw, setCurrent persists, list stable) |
| SR-55 (new) | Player configuration defaults satisfy game rules | `PlayerConfig.defaults()` factory in `maze-common-backend` | `PlayerConfigTest` (9 tests: health>0, speed>0, enabled=true, matches PlayerConstants, non-blank id/displayName, non-null image paths, toString/equality/hashCode) |
| SR-56 (new) | Difficulty picker routing logic is testable without real dialogs | `FxDifficultyPickerSupport.resolvePickResult(...)` extracted package-private method | `FxDifficultyPickerSupportTest` (4 tests: no-restart path calls onDifficultySet+onResume, hard-restart path, resume called exactly once, hardRestart called exactly once) |
| SR-57 (new) | Game-over music channel and resource are correct | `GameOverAction.startGameOverMusic()` | `GameOverActionTest` (3 tests: GAME_OVER_MUSIC channel, GameOverSound resource, 2 calls = 2 invocations) |
| SR-58 (new) | Win-game action lifecycle with partial initialization | `WinGameAction.WinGame()` | `WinGameActionTest` (2 tests: constructor no-throw, WinGame stops IN_GAME_MUSIC before scene null-check) |
| SR-59 (new) | Start-screen action with missing scene | `StartScreenAction.Load()` | `StartScreenActionTest` (3 tests: Load without scene = 0 stop calls, Load with null root no-throw, constructor no-throw) |
| SR-60 (new) | HUD coordinator message display and lifecycle | `FxHudCoordinator.setMessage(...)`, `dispose()` | `FxHudCoordinatorTest` (6 tests: setMessage updates label, empty clears, null supplier no NPE, dispose no-throw, duration sets immediately, replacing timed message updates) |
| SR-61 (new) | Path hint coordinator renders enemy routes and resets alpha | `FxPathHintCoordinator.refreshPathCanvas()`, `dispose()` | `FxPathHintCoordinatorTest` (5 tests: refreshPathCanvas no-throw, enemy drawer called when route hint not visible, globalAlpha reset to 1.0, null canvas no-throw, dispose no-throw) |
| SR-62 (new) | Zombie character construction and damage model | `ZombieCharacter` in `maze-javafx-backend` | `ZombieCharacterTest` (6 tests: constructor, getDamage positive, subtractHitPoints partial, audio on overlap, death subscriber management, getModel) |
| SR-63 (new) | PumpkinBomber character construction and damage model | `PumpkinBomberCharacter` in `maze-javafx-backend` | `PumpkinBomberCharacterTest` (8 tests: constructor, getDamage positive, subtractHitPoints partial, setHitPoints, addHitPoints, doPositionEvaluation no-throw, getModel) |
| SR-64 (new) | libGDX command classes respect terminal guard and delegate correctly | All 6 libGDX command classes (`ApplyPathHintCommand`, `ReturnToMenuCommand`, `OpenHighScoresCommand`, `ToggleSpanningTreeCommand`, `ToggleTerminalCommand`, `MovePlayerCommand`) | `GdxGameCommandsTest` (terminal guard, 2-call count, key-based routing) |
| SR-65 (new) | Combat/enemy flow support guards and win conditions | `GdxGameCombatAndEnemyFlowSupport.advanceEnemies(...)`, `shouldTriggerWin(...)`, `triggerWin(...)` | `GdxGameCombatAndEnemyFlowSupportTest` (7 tests: empty list, null maze, null player, notPlaying, playerDead, farFromGoal, triggerWin once, sets WON mode) |
| SR-66 (new) | Game-start flow viewport initialization and difficulty text | `GdxGameStartFlowApplySupport.ensureViewportInitialized(...)`, `startedDifficultyText(...)` | `GdxGameStartFlowApplySupportTest` (4 tests: existing viewport returned, null creates new, null difficulty contains "Default", always starts with "Started") |
| SR-67 (new) | Terminal command support extended edge cases | `GdxTerminalCommandSupport` edge cases | `GdxTerminalCommandSupportExtendedTest` (9 tests: null→NONE, aliases, all commands non-null, helpText has /sep, unknown has status text, help command has positive duration) |
| SR-68 (new) | Screen layout metrics are non-negative and internally consistent | `GdxGameScreenMetrics` at multiple resolutions | `GdxGameScreenLayoutExtendedTest` (14 tests: non-negative coords for 4 resolutions, hpBarHeight/bottomRowHeight/bottomBarHeight positive, hpBarBottomY within window, strip width = window width) |
| SR-69 (new) | Asset service public API is safe in headless mode | `GdxAssetService` public API only | `GdxAssetServiceExtendedTest` (7 tests: getTexture null no-throw, duplicate queueTexture no-throw, null queueTexture no-throw, dispose multiple times no-throw, loadingProgress in [0,1], getTexture headless null, updateLoading no assets true) |
| SR-70 (new) | `IUiScheduler` test double captures and replays actions in inline and deferred modes | `CapturingUiScheduler` in `maze-common-frontend` test utilities | `CapturingUiScheduler` (inline/deferred modes, `callCount()`, `capturedActions()`, `flush()`; used as shared test infrastructure for UI-thread-dispatching coordinators) |
| SR-71 (new) | `FxEnemyCoordinator` lifecycle is safe with null-returning suppliers | `FxEnemyCoordinator` constructor and public lifecycle methods in `maze-javafx-backend` | `FxEnemyCoordinatorTest` (6 tests: stepAll no-throw, reset no-throw, showEnemyDebugLabels null board, drawEnemyNavigationPaths null maze, dispose no-throw, dispose idempotent) |
| SR-72 (new) | `GdxGameRenderPipeline` static helpers are correct for all null combinations and GameMode values | `GdxGameRenderPipeline.hasRenderableWorld(...)`, `isHighScoresOnlyOverlay(...)` in `maze-libgdx` | `GdxGameRenderPipelineExtendedTest` (9 tests: all-present returns true, 4 null combinations return false, HIGH_SCORES without world true, HIGH_SCORES with world false, all non-HIGH_SCORES modes false via @ParameterizedTest @EnumSource EXCLUDE) |

| BUG-1 (WallMaterialBaseType classpath) | Correct Maven packaging (jar not eclipse-plugin), defensive guard | `main.game.maze.walls` packaging changed from `eclipse-plugin` to `jar`; explicit dep in both consumer poms; `FxGameSessionBootstrapper.setup()` and `RuntimeVisualModelLoader.resolveWallDefinition()` both catch `ExceptionInInitializerError/NoClassDefFoundError` and fall back gracefully | `FxMazeCanvasRendererTest.wallRegistryInitialisesWithoutError`, `FxMazeCanvasRendererTest.wallRegistryHasAtLeastOneMaterial`, `FxMazeCanvasRendererTest.wallRegistryGetReturnsKnownWall`, `FxMazeCanvasRendererTest.drawCanvasEmptyListReturnsCanvas`, `FxMazeCanvasRendererTest.drawCanvasUnknownDifficultyFallsBack`, `FxMazeCanvasRendererTest.drawCanvasNullDifficultyNameFallsBack` |
| BUG-2 (spawnByTarget single-attempt) | Full-candidate retry (correctness), shuffle randomisation | `OpponentRuntimeFactory.spawnByTarget`: replaces single random-pick + immediate `break` with `Collections.shuffle` + iterate-all loop; first fitting candidate picked, remaining budget re-checked per slot | `OpponentRuntimeFactorySpawnTest.allFittingCandidatesFillsAllSlots`, `OpponentRuntimeFactorySpawnTest.mixedCandidatesAlwaysPicksFittingOne`, `OpponentRuntimeFactorySpawnTest.noFittingCandidateSpawnsZero`, `OpponentRuntimeFactorySpawnTest.capEnforcementLimitsSpawnCount`, `OpponentRuntimeFactorySpawnTest.multiTypeSpawningFillsBothTypes`, `OpponentRuntimeFactorySpawnTest.instantiateFromModelSpawnsEnemies` |
| GR-36 (death penalty = 5 000) | Shared constant, single source of truth (DRY, SRP) | `StageConstants.ScoreDeathPenalty = 5000`; `ScoringEngine.characterScreenScore(...)` applies it when `currentHp <= 0`; libGDX `GdxGameScreenController.currentScore()` dispatches to `GdxGameRuntimeSupport.endScreenScore(...)` when mode is GAME_OVER or WON; `PlayerCombatStateService.currentHitPointsAsInt()` and `maxHitPoints()` expose the runtime HP to the scoring path | `ScoringEngineTest.deathPenaltyConstantIs5000`, `ScoringEngineTest.characterScreenScoreAppliesDeathPenaltyOf5000WhenDead`, `ScoringEngineTest.characterScreenScoreDeathPenaltyAbsentWhenAlive`, `GdxGameRuntimeSupportTest.endScreenScoreAppliesDeathPenaltyOf5000WhenDead`, `GdxGameRuntimeSupportTest.endScreenScoreDiffersFromGameplayScoreByDeathPenaltyWhenDead`, `EndScreenScoringParityTest.sharedDeathPenaltyConstantIs5000`, `EndScreenScoringParityTest.libgdxAndJavafxProduceIdenticalEndScreenScore`, `EndScreenScoringParityTest.deathPenaltySeparatesDeadFromSurvivedScore`, `PlayerCombatStateServiceTest.currentHitPointsAsIntReturnsMaxHpAfterReset`, `PlayerCombatStateServiceTest.maxHitPointsReturnsValueFromReset`, `PlayerCombatStateServiceTest.currentHitPointsAsIntIsZeroAfterLethalDamage` |
| GR-37 (damage × 10 on end screens) | Shared scorer, DRY parity across frontends | `ScoringEngine.characterScreenScore(...)` subtracts `(maxHp - currentHp) * ScoreSubtractFactor` (damage × 10); both JavaFX (`CharacterActionScreens.updateScore()`) and libGDX (`GdxScoreSupport.endScreenScore(...)`) call `characterScreenScore` on GAME_OVER / WON overlays | `ScoringEngineTest.damagePenaltyIsExactlyDamageTimes10`, `ScoringEngineTest.noDamageNoPenaltyDifference`, `ScoringEngineTest.characterScreenScoreAppliesDamageTimesTenOnWin`, `GdxGameRuntimeSupportTest.endScreenScoreAppliesDamageTimesTenWhenDamaged`, `GdxGameRuntimeSupportTest.endScreenScoreAddsWinBonusWhenWon`, `EndScreenScoringParityTest.damagePenaltyOnEndScreenIs10PerHpPoint`, `EndScreenScoringParityTest.damagePenaltyAbsentWhenNoHpLost`, `PlayerCombatStateServiceTest.currentHitPointsAsIntDecreasesAfterDamage` |
| GR-36 + GR-37 speed multiplier parity | XMI-sourced difficulty multipliers applied identically across frontends (DRY) | `EnemySpawnPlanner.applySpeedMultiplier(baseSpeed, mult)`; multiplier values (Easy 0.80, Normal 1.00, Hard 1.15) read from `difficulties.xmi`; both `RuntimeVisualModelLoader` (libGDX) and `OpponentRuntimeFactory` (JavaFX) delegate to the planner | `DifficultyXmiParameterizedParityTest` §4 SpeedMultiplier — `libgdx_spawnedSpeedMatchesFormula`, `javafx_applyDifficultyAttributesScalesSpeed`, `bothFrontends_speedAgreesForEveryNamedEnemy`, `enemiesAreAtLeastAsSpeedyAsEasy`, `libgdx_enemiesAreSpawnedAtLeastAsSpeedyAsEasy` |
| F10, F20 (Projectile variants) | Strategy by projectile type, SRP runtime model propagation, DRY cross backend parity | JavaFX `PumpkinBomberCharacter` projectile strategy flow (`STRAIGHT`, `LOB`, `BEAM`) plus `FxEnemyCoordinator` attack/update loop; libGDX `EnemySpawn` ranged fields propagated by `RuntimeVisualModelLoader`, runtime simulation in `GdxEnemyRuntime.updateRangedAttacks(...)`, frame integration in `GdxGameCombatAndEnemyFlowSupport.updateCombat(...)`, visual overlay rendering in `GdxGameWorldView` through render-state propagation | `GdxEnemyRuntimeProjectileTest.straightProjectileStopsAtWallBeforePlayer`, `GdxEnemyRuntimeProjectileTest.lobProjectileIgnoresWallAndAppliesSplashAtTarget`, `GdxEnemyRuntimeProjectileTest.beamDealsInstantDamageWithoutTravelTime`, plus regression guard `RuntimeVisualModelLoaderTest.loadsPlayerGoalWallAndEnemyDataFromSharedModels` |
| F26 (Ranged projectile speed) | Model-driven physics, DRY single source of truth, cross-frontend parity | `RangedEnemy.projectileSpeed` (opponents.ecore) → `RuntimeVisualModelLoader.projectileSpeedFor()` → `EnemySpawn.projectileSpeed()` → libGDX `GdxEnemyRuntime`: `duration = distance / speedValue`, `progress += dt / duration`; JavaFX `PumpkinBomberCharacter`: `duration = dist / speed`, `u += dt / duration`. No hardcoded flight speed in either backend. | libGDX: `GdxEnemyRuntimeProjectileTest.lobProjectile_positionAfterOneSecond_equalsProjectileSpeedPixelsFromStart` — asserts distance traveled after 1 s equals `projectileSpeed`; JavaFX: `PumpkinBomberCharacterTest.lobProjectile_arrivesAfterDistanceDividedBySpeed_seconds`, `PumpkinBomberCharacterTest.lobProjectile_stillInFlightBeforeDistanceDividedBySpeed_seconds` — assert arrival at exactly `distance/speed` seconds |

## Quality Attributes

1. Maintainability
Code centralizes movement decisions in shared services and exposes a minimal
overlay DTO (`ActivePathPoint`) to keep frontend drawing code thin. Ghost
phasing logic is fully encapsulated in `GhostNonTangibilityService` and
`GhostPhasingMovementService`; both frontends delegate to them.

2. Testability
Movement services are pure enough to execute against `WorldView` stubs and
assert deterministic behavior windows. Ghost phasing services are tested via
unit tests with controlled inputs. New test-double infrastructure
(`CapturingAudioEngine`, `FakeWorldView`, `SpyActionSink`, `CapturingUiScheduler`) isolates
audio-engine, scene-graph, and UI-thread-dispatch dependencies so coordinator
and action tests run without a real game loop.

3. Parity and correctness
JavaFX and libGDX consume the same movement runtime state for enemy path
overlay rendering. Ghost phasing opacity and energy drain are guaranteed
identical via a shared service.

4. Reliability / bug prevention
A cap-awareness bug in `CompositionResolverImpl.redistributeToTotal(...)` was
caught by the new `CompositionResolverImplTest.resolve_capsAreRespected` test
and fixed: the redistribution loop now skips types already at their cap
rather than adding back to them, guaranteeing caps are the final authority.
